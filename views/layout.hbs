<!DOCTYPE html>
<html>
  {{> head}}
  <body>
	{{> nav}}
	<div class="row">
		<div class="conteneur col-md-8"> 
			{{{body}}}
		</div>
		{{#compare map true operator='=='}}
			{{>olCentre}}
		{{/compare}}
		
	</div>
	{{>endBootstrap}}
  </body>
</html>
