<div class="conteneur">
<h1>{{title}}</h1>
<br>
<select name="users" id="user">
           <option value="0" >Select a user to modify his datas</option>
        {{#each users}}
            <option value="{{this._id}}" >{{this.name}} {{this.firstName}} - {{this.function}} {{this.login}}</option>
        {{/each}}
</select>
<h2>or</h2><br/>
<button id="newUser">Create a new user</button>
<h2>or</h2><br/>
<select name="users" id="delUsers">
           <option value="0" >Selctionnez un user à effacer</option>
        {{#each users}}
            <option value="{{this._id}}" >{{this.name}} {{this.firstName}}</option>
        {{/each}}
</select>
</div>
<script>
    var usr = document.getElementById('user');
    usr.addEventListener('change', function(evt) {
       window.location = "/formUser/"+usr.value;
    });
    var btn_new = document.getElementById('newUser');
    btn_new.addEventListener('click', function(evt) {
       window.location = "/newUser";
    });
    var del_User = document.getElementById('delUsers');
    del_User.addEventListener('change', function(evt) {
		if(confirm('Etes vous sur?')){
       window.location = "/delUser/"+del_User.value;
       }else{window.location="/users"}
    });
</script>
