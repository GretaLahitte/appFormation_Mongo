<h1>{{title}}</h1>
<form methode="POST" action="{{form_action}}" id="formAddFormateur">
   <input type="hidden" name="id" value="{{formateur._id}}" />
    nom : <input type="text" name="nom" value="{{formateur.nom}}" /><br/>
    prénom : <input type="text" name="prenom" value="{{formateur.prenom}}" /><br/>
    statut : <input type="text" name="statut" value="{{formateur.statut}}" /><br/>
    prix_jour : <input type="text" name="prix_jour" value="{{formateur.prix_jour}}" /><br/>
    <input type="submit" value="Confirmer la {{libelle}}" />
</form>
<!-- Script de validation du formulaire de modification -->
<script>
    var form = document.getElementById('formAddFormateur');
    form.addEventListener('submit', function(evt) {
        form.action += "/{{formateur._id}}";
        form.submit();
    });
</script>
